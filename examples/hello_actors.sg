# The simplest program that could possibly kick-start the next phase of Sophie's development.

define:
    EOL = chr(10);
    console = yes;  # Temporary hack; the word "console" needs a definition.

begin:
	console:echo [
        "All the world's a stage,", EOL,
        "And all the men and women merely Players.", EOL,
    ];

